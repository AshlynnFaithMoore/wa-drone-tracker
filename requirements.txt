
# Web server
flask==3.0.0
flask-sqlalchemy==3.1.1

# Database
sqlalchemy==2.0.23

# Data fetching & processing
requests==2.31.0
pandas==2.1.4

# Background task scheduling (auto-refresh data)
apscheduler==3.10.4

# Environment variable management
python-dotenv==1.0.0

# GIS / geographic processing (for WA county mapping)
# Installed via conda already — listed here for reference
# geopandas==0.14.1
# shapely==2.0.2

# Production WSGI server (used in Docker)
gunicorn==21.2.0

# Testing
pytest==7.4.3
pytest-cov==4.1.0